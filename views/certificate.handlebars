<div>
      
    {{#each Pessoa}}
    <h1>{{req.body.value}}<span> </span>{{lastname.lastname}}</h1>
    <h1>{{curso}}</h1>

    {{/each}}
    <form>
        <input type="button" value="Print this page" onClick="window.print()" />
    </form>
</div>